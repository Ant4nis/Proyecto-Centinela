namespace Tests
{
    /// <summary>
    /// Categorías disponibles para filtrar los mensajes de depuración.
    /// </summary>
    public enum DebugCategory
    {
        RoomGeneration,
        TileCounting,
        AI,
        UI,
        Player,
        Custom
    }
}